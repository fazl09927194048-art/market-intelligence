'use client';
import React,{useEffect,useMemo,useState} from 'react';
import styles from './MarketChart.module.css';
type Candle={time:number;open:number;high:number;low:number;close:number;volume:number;trades?:number};
type Trade={price:number;qty:number;value:number;time:number;id:number;side:'BUY'|'SELL'};
type Props={symbol:string;interval:string;fa?:boolean};
const money=(v:number)=>Number.isFinite(v)?new Intl.NumberFormat('en-US',{maximumFractionDigits:v>=100?2:v>=1?4:8}).format(v):'—';
const toCandle=(row:any):Candle|null=>{if(!row||typeof row!=='object')return null;const n=[row.time,row.open,row.high,row.low,row.close,row.volume,row.trades].map(Number);if(n.slice(0,6).some(x=>!Number.isFinite(x)))return null;return{time:n[0],open:n[1],high:n[2],low:n[3],close:n[4],volume:n[5],trades:Number.isFinite(n[6])?n[6]:undefined};};
const streamInterval=(v:string)=>v==='1d'?'1d':v==='4h'?'4h':v==='1h'?'1h':v==='5m'?'5m':'15m';
export default function MarketChart({symbol,interval,fa=false}:Props){
 const [candles,setCandles]=useState<Candle[]>([]),[trades,setTrades]=useState<Trade[]>([]),[book,setBook]=useState<any>(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[hover,setHover]=useState<Candle|null>(null),[live,setLive]=useState(false);
 useEffect(()=>{
  let dead=false;let ws:WebSocket|null=null;let reconnect:number|undefined;let fallbackTimer:number|undefined;
  const loadSnapshot=async()=>{try{const [kc,ob,tr]=await Promise.all([fetch(`/api/klines?symbol=${encodeURIComponent(symbol)}&interval=${encodeURIComponent(interval)}&limit=100`,{cache:'no-store'}),fetch(`/api/orderbook?symbol=${encodeURIComponent(symbol)}&limit=20`,{cache:'no-store'}),fetch(`/api/trades?symbol=${encodeURIComponent(symbol)}&limit=80`,{cache:'no-store'})]);const [kd,od,td]=await Promise.all([kc.json(),ob.json(),tr.json()]);const parsed=(kd.candles||[]).map(toCandle).filter(Boolean) as Candle[];if(!parsed.length)throw new Error(fa?'کندل زنده دریافت نشد':'Live candles unavailable');if(!dead){setCandles(parsed);setBook(od);setTrades(Array.isArray(td.trades)?td.trades:[]);setError('');setLoading(false)}}catch(e){if(!dead){setError(e instanceof Error?e.message:(fa?'داده بازار در دسترس نیست':'Market data unavailable'));setLoading(false)}}};
  const connect=()=>{if(dead)return;setLive(false);const s=symbol.toLowerCase(),iv=streamInterval(interval);const streams=[`${s}@kline_${iv}`,`${s}@aggTrade`,`${s}@depth20@100ms`,`${s}@bookTicker`].join('/');ws=new WebSocket(`wss://data-stream.binance.vision/stream?streams=${streams}`);ws.onopen=()=>{if(dead)return;setLive(true);setError('')};ws.onmessage=e=>{if(dead)return;try{const packet=JSON.parse(e.data);const d=packet.data||packet;const type=d.e;const now=Date.now();if(type==='kline'&&d.k){const k=d.k;const c: Candle={time:Number(k.t),open:Number(k.o),high:Number(k.h),low:Number(k.l),close:Number(k.c),volume:Number(k.q),trades:Number(k.n)};if([c.time,c.open,c.high,c.low,c.close,c.volume].every(Number.isFinite))setCandles(prev=>{const next=prev.slice();const i=next.findIndex(x=>x.time===c.time);if(i>=0)next[i]=c;else next.push(c);return next.slice(-100)})}else if(type==='aggTrade'){const price=Number(d.p),qty=Number(d.q);if(Number.isFinite(price)&&Number.isFinite(qty)){const t:Trade={price,qty,value:price*qty,time:Number(d.T)||now,id:Number(d.a),side:d.m?'SELL':'BUY'};setTrades(prev=>[...prev,t].slice(-120))}}else if((type==='depthUpdate'||type===undefined)&&Array.isArray(d.bids)&&Array.isArray(d.asks)){const bids=d.bids.map((x:any)=>({price:Number(x[0]),quantity:Number(x[1]),value:Number(x[0])*Number(x[1])})).filter((x:any)=>Number.isFinite(x.price)&&Number.isFinite(x.quantity));const asks=d.asks.map((x:any)=>({price:Number(x[0]),quantity:Number(x[1]),value:Number(x[0])*Number(x[1])})).filter((x:any)=>Number.isFinite(x.price)&&Number.isFinite(x.quantity));if(bids.length&&asks.length){const buyValue=bids.reduce((a:number,x:any)=>a+x.value,0),sellValue=asks.reduce((a:number,x:any)=>a+x.value,0),sum=buyValue+sellValue;setBook({bids,asks,buyValue,sellValue,buyPct:sum?buyValue/sum*100:null,sellPct:sum?sellValue/sum*100:null,imbalance:sum?(buyValue-sellValue)/sum*100:null,lastUpdateId:d.u||d.lastUpdateId,source:'Binance WebSocket'})}}else if(type==='bookTicker'){setBook((prev:any)=>prev?{...prev,bids:prev.bids?.length?[{price:Number(d.b),quantity:Number(d.B),value:Number(d.b)*Number(d.B)},...prev.bids.slice(1)]:prev.bids,asks:prev.asks?.length?[{price:Number(d.a),quantity:Number(d.A),value:Number(d.a)*Number(d.A)},...prev.asks.slice(1)]:prev.asks}:prev)}}catch{}};ws.onerror=()=>{if(!dead)setLive(false)};ws.onclose=()=>{if(!dead){setLive(false);reconnect=window.setTimeout(connect,1000)}}};
  loadSnapshot();connect();fallbackTimer=window.setInterval(()=>{if(!dead&&!ws||ws?.readyState!==WebSocket.OPEN)loadSnapshot()},10000);
  return()=>{dead=true;if(reconnect)window.clearTimeout(reconnect);if(fallbackTimer)window.clearInterval(fallbackTimer);ws?.close()};
 },[symbol,interval,fa]);
 const geom=useMemo(()=>{if(!candles.length)return null;const width=1000,height=300,pad=18,min=Math.min(...candles.map(c=>c.low)),max=Math.max(...candles.map(c=>c.high)),range=Math.max(max-min,Math.abs(max)*0.000001),step=(width-pad*2)/candles.length,y=(v:number)=>pad+(max-v)/range*(height-pad*2);return{width,height,min,max,step,y}},[candles]);
 const buy=trades.filter(t=>t.side==='BUY'),sell=trades.filter(t=>t.side==='SELL'),buyVol=buy.reduce((a,t)=>a+t.value,0),sellVol=sell.reduce((a,t)=>a+t.value,0),total=buyVol+sellVol,buyPct=total?buyVol/total*100:0,sellPct=total?sellVol/total*100:0;
 const spread=book?.bids?.[0]&&book?.asks?.[0]?Number(book.asks[0].price)-Number(book.bids[0].price):null;
 const estimatedBuyers=Math.max(1,Math.round(buy.length/4)),estimatedSellers=Math.max(1,Math.round(sell.length/4));
 return <section className={styles.chart} aria-label="Live crypto trading terminal"><div className={styles.head}><div><b>{symbol.replace('USDT','/USDT')} · {interval}</b><small>{loading?'SYNCING':error||`${candles.length} OHLC candles · ${live?'LIVE WS':'REST FALLBACK'}`}</small></div><div className={styles.value}>{hover?`${money(hover.close)} · H ${money(hover.high)} · L ${money(hover.low)}`:candles.length?money(candles[candles.length-1].close):'—'}</div></div><div className={styles.canvas}>{geom?<svg viewBox={`0 0 ${geom.width} ${geom.height}`} preserveAspectRatio="none"><path d={`M 18 ${geom.y((geom.min+geom.max)/2)} H ${geom.width-18}`} className={styles.grid}/>{candles.map((c,i)=>{const x=18+i*geom.step+geom.step/2,up=c.close>=c.open,top=geom.y(Math.max(c.open,c.close)),bottom=geom.y(Math.min(c.open,c.close));return <g key={`${c.time}-${i}`} onMouseEnter={()=>setHover(c)} onMouseLeave={()=>setHover(null)} className={`${styles.candle} ${up?styles.up:styles.down}`}><line x1={x} x2={x} y1={geom.y(c.high)} y2={geom.y(c.low)}/><rect x={x-Math.max(1,geom.step*.29)} y={top} width={Math.max(2,geom.step*.58)} height={Math.max(1,bottom-top)} rx="1"/></g>})}</svg>:!loading?<div className={styles.empty}>{error||'No chart data'}</div>:null}</div><div className={styles.volume}><div className={styles.volHead}><b>VOLUME FLOW</b><span>{money(total)} · {trades.length} observed trades</span></div><div className={styles.bar}><i style={{width:`${buyPct}%`}}/><em style={{width:`${sellPct}%`}}/></div><div className={styles.volStats}><span>BUY <b>{buyPct.toFixed(1)}%</b> · {money(buyVol)}</span><span>SELL <b>{sellPct.toFixed(1)}%</b> · {money(sellVol)}</span></div></div><div className={styles.stats}><div><small>BUY TRADES</small><b>{buy.length}</b></div><div><small>SELL TRADES</small><b>{sell.length}</b></div><div><small>EST BUYERS*</small><b>~{estimatedBuyers}</b></div><div><small>EST SELLERS*</small><b>~{estimatedSellers}</b></div><div><small>BOOK BUY</small><b>{money(Number(book?.buyValue))}</b></div><div><small>BOOK SELL</small><b>{money(Number(book?.sellValue))}</b></div><div><small>IMBALANCE</small><b>{book?.imbalance==null?'—':`${Number(book.imbalance).toFixed(1)}%`}</b></div><div><small>SPREAD</small><b>{spread==null?'—':money(spread)}</b></div></div><div className={styles.tape}><div className={styles.tapeHead}><b>LIVE TRADE TAPE</b><span>{live?'WebSocket · real-time':'REST fallback'}</span></div>{trades.slice(-12).reverse().map((t,i)=><div className={styles.trade} key={`${t.id}-${i}`}><b className={t.side==='BUY'?styles.buy:styles.sell}>{t.side}</b><span>{money(t.price)}</span><span>{money(t.value)}</span><small>{new Date(t.time).toLocaleTimeString()}</small></div>)}</div><div className={styles.note}>{fa?'شناسه کاربران عمومی نیست؛ بنابراین تعداد خریداران/فروشندگان منحصربه‌فرد قابل مشاهده نیست. برآورد فقط یک تقریب شفاف بر اساس معاملات مشاهده‌شده است.':'Exact unique users cannot be counted from public exchange data because account IDs are not exposed. *Estimated buyers/sellers are only a heuristic based on observed trades, not unique user IDs.'}</div><div className={styles.foot}><span>LIVE OHLCV + ORDER FLOW</span><span>Binance public market data · WS first, REST fallback</span></div></section>;
}
